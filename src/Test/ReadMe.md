# Calculator Contract Tests

- Ensure a local HotPocket cluster is running and exposing `wss://localhost:8081`.
- Run tests from repository root with:

```bash
npm test
```

This will execute `src/Test/run-tests.js`.
